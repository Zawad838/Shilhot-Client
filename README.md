
  # Convert to React App

  This is a code bundle for Convert to React App. The original project is available at https://www.figma.com/design/UIlusFLGiGaCFH2JVg2wsU/Convert-to-React-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  